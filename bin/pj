#!/usr/bin/env ruby

require 'pj'
require 'optparse'
options = {}
OptionParser.new do |opts|
  opts.on("-dDEPTH", "--indent-depthDEPTH", "Indent Depth") do |d|
   options[:d] = d 
  end
end.parse!

input = ARGV[0] || ARGF.read
puts PJ::prettify(input, options)

